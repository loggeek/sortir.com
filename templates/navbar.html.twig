<nav class="navbar navbar-expand-lg" style="background-color: whitesmoke">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_home') }}">
            <img src="{{ asset('logo.png') }}" alt="sortir.com" height="32px">
        </a>
    </div>
    <div class="collapse navbar-collapse">
        <ul class="navbar-nav">
            {% if app.user %}
                {% if app.user.isadmin %}
                    <li class="nav-item d-flex me-2"><a class="nav-link text-success" href="{{ path('admin_ville') }}">Villes</a></li>
                    <li class="nav-item d-flex me-2"><a class="nav-link text-success" href="{{ path('admin_campus') }}">Campus</a></li>
                {% endif %}
                <li class="nav-item d-flex me-2"><a class="nav-link" href="{{ path('app_home') }}">Accueil</a></li>
                <li class="nav-item d-flex me-2"><a class="nav-link" href="{{ path('profil.edit') }}">Profil</a></li>
                <li class="nav-item d-flex me-2"><a class="nav-link text-danger" href="{{ path('app_logout') }}">Déconnexion</a></li>
            {% endif %}
        </ul>
    </div>
</nav>